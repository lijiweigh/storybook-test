import { ArgsTable, Meta, Story, Canvas, PRIMARY_STORY } from '@storybook/addon-docs/blocks'
import { Button } from './Button.tsx'

<Meta title='MDX/Button222' />

export const Template = (args) => <Button {...args} />

# demo

<Story
  name='Primary'
  argTypes={{
    backgroundColor: { control: 'color' },
    label: {
      name: 'label',
      type: { name: 'string', required: true },
      defaultValue: 'Hello',
      description: 'demo description ooo',
      table: {
        type: { summary: 'short', detail: 'long detail' },
        defaultValue: { summary: 'short', detail: 'long detail' }
      },
      control: {
        type: 'text'
      }
    }
  }}
  args={{
    primary: true,
    label: 'Primary'
  }}
>
  {Template.bind({})}
</Story>

<Canvas>
  <Story
    name='Large'
    args={{
      size: 'large',
      label: 'Button'
    }}
  >
    {(args) => <Button {...args} />}
  </Story>
</Canvas>

<ArgsTable story={PRIMARY_STORY} />
